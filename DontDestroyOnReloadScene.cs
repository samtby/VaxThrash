using UnityEngine;
using UnityEngine.SceneManagement;

public class DontDestroyOnReloadScene : MonoBehaviour
{
 
    //public GameObject[] objects;

    //public static DontDestroyOnReloadScene instance;

    private void Awake()
    {
        //if(instance != null)
        //{
           // Debug.LogWarning("+ d'une instance de DontDestroyOnLoadScene la scène");
            //return;
        //}
        
        //instance = this;

        //foreach (var element in objects)
        //{
            //DontDestroyOnLoad(element);
        //}
    }
}