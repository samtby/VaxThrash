using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChronoStartAnimation : MonoBehaviour
{

    //public Animation chrono;

    // Start is called before the first frame update
    
    void Start()
    {
        //Animation du Chrono
        //chrono = GetComponent<Animation>();
        //chrono.gameObject.GetComponent<Animation>().enabled = false;
    }
  void Update()   
    {
        //Si le joueur appuie sur n'importe quelle touche
        //if (Input.anyKey)
        //{
          //  chrono.gameObject.GetComponent<Animation>().enabled = true;
        //}
    }
}
